# 프로젝트 전체 구상

> 계획서(정보과학 프로젝트) 내용을 바탕으로, 실제 구현 관점에서 정리한 문서.
> 진행하면서 계속 갱신한다.

## 1. 한 줄 정의

사용자가 관심 있는 선수·팀을 고르면 관련 이적설 기사를 모아 보여주되, 각 기사를 쓴
기자·매체가 얼마나 신뢰할 만한지 함께 표시해 주는 서비스.

## 2. 전체 아키텍처

```
1~5차시: Next.js 프론트만으로 완결된 서비스 (더미 데이터 배열 사용)
           ↓
6~8차시: Supabase(Postgres)를 붙여서 더미 데이터를 실제 DB 데이터로 교체
```

- **프레임워크**: Next.js (App Router) + TypeScript
  - 컴포넌트 단위로 화면을 쪼개서 만들기 쉽고, Vercel 배포가 매우 간단해서 5차시 목표(첫 배포)에 적합.
- **스타일**: Tailwind CSS — 클래스만으로 빠르게 UI를 만들 수 있어 매 차시 진도를 맞추기 좋음.
- **배포**: Vercel (5차시).
- **DB**: Supabase (6차시~) — Postgres 기반, 테이블 설계가 계획서의 "저장할 데이터" 항목과 1:1로 대응됨.
- **상태 관리**: 별도 라이브러리 없이 React `useState` + props로 충분 (규모가 작음).

### 폴더 구조 (현재)

```
app/
  layout.tsx       # 전체 레이아웃, 메타데이터
  page.tsx         # 메인 화면
  globals.css
components/
  Header.tsx       # 상단 바 (제목)
  SearchBar.tsx     # 검색창
  FilterTabs.tsx    # 최신순/팀별/기자별/선수별 필터 버튼
docs/
  PLAN.md          # 이 문서
```

앞으로 늘어날 구조 (예정):

```
app/
  rumor/[id]/page.tsx   # 4차시: 이적설 상세 페이지
  admin/page.tsx        # 관리자 기사 등록 폼
components/
  RumorCard.tsx          # 3차시: 이적설 카드
  ArticleTimelineItem.tsx # 4차시: 상세 페이지 타임라인 항목
lib/
  dummyData.ts           # 3차시: 더미 선수/팀/기자/기사 데이터
  trustScore.ts           # 5차시: 신뢰도 계산 함수
  supabase.ts              # 6차시: Supabase 클라이언트
```

## 3. 데이터 모델 (계획서 2번 항목 기준)

3개 테이블. 3~5차시는 이 구조를 그대로 TypeScript 타입 + 더미 배열로 먼저 만들고,
6차시에 Supabase 테이블로 옮긴다.

### `journalists` (기자)

| 필드 | 설명 |
|---|---|
| id | PK |
| name | 기자 이름 |
| outlet | 소속 매체 |
| league_tiers | `{ league: string, score: number }[]` — 리그별 신뢰도 티어 점수 |
| base_tier | 기본 신뢰도 티어 점수 (담당 리그 기록이 없을 때 대신 사용) |

### `articles` (기사)

| 필드 | 설명 |
|---|---|
| id | PK |
| url | 원문 링크 |
| journalist_id | FK → journalists |
| player_name | 관련 선수명 |
| from_team / to_team | 이적 전 / 이적 후 팀 |
| certainty | 확실성 카테고리: `확정` \| `진전` \| `관심` |
| summary | 요약 코멘트 |
| published_at | 등록 날짜 |
| score | 계산된 개별 신뢰 점수 (파생값) |

### `rumors` (이적설)

| 필드 | 설명 |
|---|---|
| id | PK |
| player_name | 관련 선수명 |
| article_ids | 관련 기사 id 목록 |
| aggregate_score | 종합 신뢰 점수 (파생값) |
| updated_at | 최종 업데이트 날짜 |

## 4. 신뢰도 계산 로직 (계획서 2번 "처리" 항목 기준)

1. 기사 등록 시 선수/팀/기자/날짜 기준으로 태그 부여.
2. `(기자, 기사에 태그된 리그)` 조합으로 `journalists.league_tiers`에서 티어 점수 조회.
   - 해당 리그 기록이 없으면 `base_tier` 사용.
3. `개별 기사 신뢰 점수 = 티어 점수 × 확실성 가중치`
   - 확실성 가중치(초기안, 조정 예정): `확정 = 1.0`, `진전 = 0.7`, `관심 = 0.4`
4. `종합 신뢰 점수 = 같은 이적설에 속한 기사들의 개별 점수 평균`
   - 1단계: 단순 평균으로 구현 (5차시 목표)
   - 2단계 개선: 최신 기사에 가중치를 더 주는 방식으로 발전 (계획서 5번 "대비 방법"과 동일)

이 계산 함수는 `lib/trustScore.ts`에 순수 함수로 작성해서, 더미 데이터든 실제 DB
데이터든 동일하게 재사용한다 (3~5차시는 더미 배열을 넣고, 8차시에 실제 DB 조회
결과를 넣기만 하면 되도록).

## 5. 화면 구성

계획서 3번 스케치 기준, 핵심 화면 3개:

1. **메인 화면**: 검색창, 필터 버튼(최신순/팀별/기자별/선수별), 선수별 이적설 카드 목록.
2. **상세 화면**: 특정 이적설의 관련 기사를 날짜순 타임라인으로 표시 (기자/매체/확실성/개별 점수/원문 링크).
3. **관리자 기사 등록 화면**: 기사 링크·기자 이름·관련 선수·관련 팀·확실도 입력 폼.

## 6. 차시별 계획 (계획서 4번, 원안 그대로)

| 차시 | 단계 | 목표 |
|---|---|---|
| 1 | 준비 | GitHub 저장소 생성, 선수·팀 목록 정하기, 기자 후보 5명 조사 |
| 2 | 프론트 | 검색창·필터 버튼 UI 클릭 가능한 상태까지 구현 (기능 없이 UI만) |
| 3 | 프론트 | 더미 데이터 배열 → 카드 목록 렌더링, 필터 버튼으로 실제 정렬 |
| 4 | 프론트 | 카드 클릭 → 상세 페이지, 기사 더미 데이터를 날짜순 타임라인으로 |
| 5 | 프론트 | 신뢰도 계산 함수 작성 + 화면 반영, Vercel 첫 배포 |
| 6 | 서버·DB | Supabase 프로젝트 생성, 3개 테이블 설계·생성, 앱과 연결 |
| 7 | 서버·DB | 관리자 등록 폼 → articles 테이블 저장 기능 |
| 8 | 서버·DB | select 쿼리로 실제 DB 데이터 조회 → 신뢰도 로직과 연결, 완성 |

## 7. 다룰 선수·팀 목록 (1차시 결과물)

더미 데이터(3차시)와 이후 화면 예시 전반에서 재사용할 기본 목록. 실제 이적 여부와
무관하게 "다양한 리그·확실성 단계"를 보여주기 위한 예시용 목록이라 여기서 자유롭게
확장/교체 가능.

**팀 (10, 5개 리그에서 2팀씩 — 리그별 필터 테스트에 유용)**

- 프리미어리그: 맨체스터 시티, 토트넘 홋스퍼
- 라리가: 레알 마드리드, FC 바르셀로나
- 세리에 A: 유벤투스, AC 밀란
- 분데스리가: 바이에른 뮌헨, 보루시아 도르트문트
- 리그 1: 파리 생제르맹, AS 모나코

**선수 (15, 팀당 1~2명 예시)**

사비뉴, 손흥민, 킬리안 음바페, 라민 야말, 두산 블라호비치, 라파엘 레앙,
해리 케인, 자말 무시알라, 우스만 뎀벨레, 폴리시치, 콜 파머, 부카요 사카,
모하메드 살라, 페더리코 발베르데, 카일 워커

## 8. 신뢰도 티어표에 넣을 기자 후보 5명 (1차시 결과물)

축구 이적 시장에서 공신력이 높거나(또는 대조적으로 낮다고 커뮤니티에서 자주
언급되는) 기자들로 초안을 구성. 계획서 5번의 대비 방법대로, 이 초기 티어는
가설이며 실제 서비스를 써보면서 점수를 조정해 나간다.

| 기자 | 소속 매체 | 주 담당 리그 | 초기 평판 메모 |
|---|---|---|---|
| 파브리지오 로마노 (Fabrizio Romano) | 프리랜서 (Here We Go) | 전 리그 공통 | 세계적으로 가장 널리 인용되는 이적 전문 기자. "Here We Go" 표현으로 확정 보도의 신호로 통용됨 → 초기 base_tier 최상위 |
| 데이비드 오른스타인 (David Ornstein) | The Athletic | 프리미어리그 | 프리미어리그·잉글랜드 대표팀 관련 정확도가 특히 높다고 평가받음 → PL 티어 최상위 |
| 잔루카 디 마르치오 (Gianluca Di Marzio) | Sky Sport Italia | 세리에 A | 이탈리아 이적 시장 전문, 세리에 A 관련 신뢰도 높음 |
| 플로리안 플레텐베르크 (Florian Plettenberg) | Sky Sport DE | 분데스리가 | 독일 이적 시장 전문, 분데스리가 관련 신뢰도 높음 |
| 던컨 카슬스 (Duncan Castles) | 프리랜서 (팟캐스트 등) | 전 리그 공통 | 축구 팬 커뮤니티에서 예측이 자주 빗나간다고 지적받는 사례로 자주 언급됨 → 의도적으로 낮은 티어 예시로 포함 (신뢰도 편차를 보여주는 대조군) |

> 참고: 이 표는 첫 초안이며, 실제 순위/점수는 축구 전문 매체·커뮤니티의 기자 신뢰도
> 관련 자료를 더 찾아본 뒤 2~3차시 사이에 구체적인 점수(0~100 등)로 확정할 예정.
